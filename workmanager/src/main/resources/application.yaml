spring:
  profiles:
    active: local
  jpa:
    properties:
      hibernate:
        open-in-view: false

jwt:
  secret: ${JWT_SECRET}
  access-token-expiration: 900000      # 15분 (ms)
  refresh-token-expiration: 604800000  # 7일 (ms)